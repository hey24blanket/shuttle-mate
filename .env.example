# Server-only. Never prefix these with NEXT_PUBLIC_.
FIREBASE_SERVICE_ACCOUNT_JSON=
FIREBASE_DATABASE_URL=
# Firebase browser config; these two are intentionally returned by /api/config.
FIREBASE_PROJECT_ID=
FIREBASE_WEB_API_KEY=
